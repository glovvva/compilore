Transform the following answer into a Mermaid flowchart showing relationships between concepts.
Rules:

Output ONLY valid Mermaid syntax. No preamble, no code fences.
Start with: graph TD
Use only alphanumeric node IDs (no spaces, no special chars)
Label edges with relationship type: -->|relates to|, -->|enables|, -->|contradicts|
Maximum 12 nodes
Use [[wikilink]] slugs as node labels where available
