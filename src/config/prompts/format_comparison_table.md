Transform the following answer into an HTML comparison table.
Rules:

Output ONLY valid HTML. No preamble. No markdown.
Use <table class="comparison-table"> as root element
First column: attribute/criterion
Subsequent columns: one per entity being compared
Use <th> for headers, <td> for cells
Add <caption> with a descriptive title
Maximum 8 rows, maximum 5 columns
