Transform the following answer into an ordered protocol.
Rules:

Output ONLY valid JSON. No preamble.
title: short string (max 12 words)
steps: array of 3–12 strings, each one imperative step, max 18 words each
Format: {"title": "...", "steps": ["...", "..."]}
