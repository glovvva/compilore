"""LangGraph: **Lint** pipeline (on-demand).

Runs orphan detection, staleness, dedup suggestions, and contradiction checks with
optional ``interrupt_before`` for HITL on conflicts — brief Sprint 3. Not for
continuous autonomous execution.
"""

from __future__ import annotations

from typing import Any


def build_lint_graph() -> Any:
    """Placeholder: return compiled LangGraph graph (implementation deferred)."""
    return None
